import { screen } from "@testing-library/react";
import { expect, test } from "vitest";

test("no keyword provided", () => {
    expect(screen.getByRole("button"))
})

test("keyword with results provided", () => {

})

test("keyword with no results provided", () => {

})